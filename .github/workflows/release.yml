name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  BIN_NAME: tabularis-mongodb-plugin

jobs:
  build:
    name: ${{ matrix.platform-label }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform-label: win-x64
            runner: windows-latest
            target: x86_64-pc-windows-msvc
          - platform-label: linux-x64
            runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - platform-label: linux-arm64
            runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
          # Cross-compiled on the Apple-Silicon runner: the macOS toolchain
          # builds x86_64 natively, avoiding the scarce free Intel (macos-13)
          # runners that can sit queued indefinitely.
          - platform-label: darwin-x64
            runner: macos-14
            target: x86_64-apple-darwin
          - platform-label: darwin-arm64
            runner: macos-14
            target: aarch64-apple-darwin

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $stage = "staging"
          New-Item -ItemType Directory -Force -Path $stage | Out-Null
          Copy-Item "target\${{ matrix.target }}\release\$env:BIN_NAME.exe" $stage
          Copy-Item "manifest.json" $stage
          Copy-Item ".tabularium" $stage
          $zip = "$env:BIN_NAME-${{ matrix.platform-label }}-${{ github.ref_name }}.zip"
          Compress-Archive -Path "$stage\*" -DestinationPath $zip
          "ASSET=$zip" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Package (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          stage=staging
          mkdir -p "$stage"
          cp "target/${{ matrix.target }}/release/$BIN_NAME" "$stage/"
          cp manifest.json "$stage/"
          cp .tabularium "$stage/"
          zip="$BIN_NAME-${{ matrix.platform-label }}-${{ github.ref_name }}.zip"
          (cd "$stage" && zip -r "../$zip" .)
          echo "ASSET=$zip" >> "$GITHUB_ENV"

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ASSET }}
          fail_on_unmatched_files: true

  # The Tabularium registry resolves a release by scanning its published assets
  # for a file literally named `.tabularium` — it does not unzip the platform
  # archives. The build job bundles the manifest *inside* each zip, so publish
  # it as a standalone asset too, otherwise the registry falls back to the
  # unsigned repo-root path (no signed integrity / per-asset SHA-256).
  manifest:
    name: Attach .tabularium manifest asset
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Upload .tabularium to the release
        uses: softprops/action-gh-release@v2
        with:
          files: .tabularium
          fail_on_unmatched_files: true
